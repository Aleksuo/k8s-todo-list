apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-frontend
  namespace: default
  labels:
    app: nginx-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-frontend
  template:
    metadata:
      labels:
        app: nginx-frontend
    spec:
      containers:
      - name: nginx-frontend
        image: dev-registry.localhost:5000/nginx-frontend